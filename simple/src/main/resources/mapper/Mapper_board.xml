<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.musunsul29.simple.db.MapperBoard">


	<insert id="insertBoard" parameterType="com.musun129.DTOBoard">
	insert into board (bo_name, bo_main, bo_time, bo_hit, bo_view) values (#{bo_name}, #{bo_main}, #{bo_time}, #{bo_hit}, #{bo_view})
	</insert>

	<select id="listBoard" parameterType="com.musun129.DTOBoard">
	select * from board where bo_view = 1
	</select>
	
	<update id="updateBoard" parameterType="com.musun129.DTOBoard">
	update board set bo_name = #{bo_name}, bo_main = #{bo_main}, bo_time = #{bo_time}, bo_hit = #{bo_hit}, bo_view = #{bo_view} where bo_pk = #{bo_pk}
	</update>
	
	<update id="deleteBoard" parameterType="com.musun129.DTOBoard">
	update board set bo_view = #{bo_view} where bo_pk = #{bo_pk}
	</update>

</mapper>