<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.musun129.Board.mapper.MapperBoard">


	<insert id="insertBoard"
		parameterType="com.musun129.Board.DTOBoard">
		insert into board (bo_name, bo_main, bo_time, bo_hit, bo_view) values
		(#{bo_name}, #{bo_main}, (now()), 0, 0)
	</insert>

	<select id="listBoard"
		resultType="com.musun129.Board.DTOBoard">
		select * from board where bo_hit = 0
	</select>

	<select id="detailBoard" parameterType="int"
		resultType="com.musun129.Board.DTOBoard">
		select * from board where bo_pk = #{bo_pk}
	</select>

	<update id="updateBoard" 
		parameterType="com.musun129.Board.DTOBoard">
		update board set bo_name = #{bo_name}, bo_main = #{bo_main}, bo_time =
		#{bo_time}, bo_hit = #{bo_hit}, bo_view = #{bo_view} where bo_pk =
		#{bo_pk}
	</update>

	<update id="deleteBoard"
		parameterType="com.musun129.Board.DTOBoard">
		update board set bo_view = #{bo_view} where bo_pk = #{bo_pk}
	</update>

	<select id="coutnBoard" parameterType="com.musun129.Board.DTOBoard" resultType="int">
		select count(*) from board
	</select>

</mapper>